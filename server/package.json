{
  "name": "ceebank-server",
  "version": "0.1.0",
  "private": true,
  "description": "CeeBank API (Node.js + Express). TypeScript and Prisma will be added step-by-step.",
  "license": "MIT",
  "scripts": {
    "start": "node dist/index.js",
    "build": "echo \"(placeholder) build step will be added\"",
    "dev": "echo \"(placeholder) dev server will be added\"",
    "test": "echo \"No tests yet\" && exit 0"
  }
}
