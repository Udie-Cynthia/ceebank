# Server
PORT=4000
NODE_ENV=development

# JWT (placeholders; real values go in your private .env)
JWT_ACCESS_SECRET=change-me-access
JWT_REFRESH_SECRET=change-me-refresh
JWT_ACCESS_EXPIRES=15m
JWT_REFRESH_EXPIRES=7d

# Database (we'll wire up PostgreSQL + Prisma later)
# For Docker/local compose you’ll eventually use something like:
# postgres://ceebank:ceebank@localhost:5432/ceebank
DATABASE_URL=postgres://USER:PASSWORD@HOST:5432/DBNAME
